# pages

This repository uses **Next.js App Router**, so top-level routes live in `app/`.

This folder exists to match the documented architecture (“pages”), but it intentionally contains **no route files** to avoid mixing routing systems.

